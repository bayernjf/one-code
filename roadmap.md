# AI Watchdog 功能迭代路线图

> 创建日期：2026-08-26
> 状态：规划中
> 前置阅读：[`handoff.md`](./handoff.md)（现状）、[`design.md`](./design.md)（原始设计方案）

## 一、背景与目标

当前核心监控能力已完整（6 种探针 + 聚合引擎 + 双形态协作），但**产品化体验有明显缺口**——桌面端像技术原型，不是日常可用的产品。

本路线图目标：从"能跑"升级为"好用"，补齐桌面端体验闭环，增加差异化能力，为正式发布做准备。

---

## 二、功能总览

| 优先级 | 功能 | 核心价值 | 状态 |
|---|---|---|---|
| **P0** | 活动历史记录 | 知道 AI 今天干了什么 | ✅ 已完成 |
| **P0** | 一键跳转/接管 | 通知点了就能回去 | ✅ 已完成 |
| **P0** | 开机自启 + 全局快捷键 | 常驻应用标配 | ✅ 已完成 |
| **P1** | 勿扰/专注模式 | 控制什么时候不打扰 | ✅ 已完成 |
| **P1** | 通知内容增强 | 通知不只是"完成了"，而是"完成了什么" | ✅ 已完成 |
| **P1** | 通知精细化控制 | 不同状态不同声音、按目标开关 | ✅ 已完成 |
| **P2** | 远程通知渠道 | 离开电脑也能收到 | ✅ 已完成 |
| **P2** | 自定义监控目标 | 支持内置 5 个之外的工具 | ✅ 已完成 |
| **P2** | 多会话状态管理 | 区分同时运行的多个 AI 任务 | ⬜ 待评估（架构改动） |
| **P3** | 统计仪表盘 | 量化 AI 使用习惯 | ⬜ 未开始 |

---

## 三、各功能详细设计

### P0-1 活动历史记录

**现状**：VS Code 扩展有 `activityPanel`（侧边栏时间线）+ `activityLog`（globalState 持久化最近 100 条）。桌面端只有一个"查看状态"弹窗，显示当前状态，**没有历史**。

**方案**：
- 桌面端新增 `ActivityLog` 模块，持久化到 `userData/activity.json`（最近 200 条）
- 每条记录：时间戳、状态（working/done/waiting）、信号来源、耗时
- 托盘菜单新增"活动历史"，打开 BrowserWindow 展示时间线列表
- 复用 core 包的 `formatDuration` 等格式化工具

**涉及模块**：新增 `desktop/src/activityLog.ts`、`desktop/src/historyWindow.ts` + 渲染页
**工作量**：中

---

### P0-2 一键跳转/接管

**现状**：VS Code 扩展有 `takeover` 命令（通知按钮 → 定位到最近改动文件末尾）。桌面端通知点击**无任何动作**。

**方案**：
- 通知点击时，根据信号来源聚焦对应应用窗口
- 来源映射：`codex` → ChatGPT.app、`claude` → Claude.app、`vscode-companion` → VS Code、`shell-hook` → 终端 App
- macOS 用 `osascript -e 'tell application "X" to activate'`
- 文件探针触发的 done，可在通知正文里列出最近改动的文件数

**涉及模块**：`main.ts` 的 `showNotification` 增加点击回调，新增 `focusApp.ts`
**工作量**：小

---

### P0-3 开机自启 + 全局快捷键

**现状**：托盘菜单只有"打开设置 / 查看状态 / 退出"。

**方案**：
- **开机自启**：Electron `app.setLoginItemSettings({ openAtLogin: true })`，设置页加开关
- **全局快捷键**：`globalShortcut.register('Alt+Cmd+W', ...)`，默认触发"显示当前状态"通知，设置页可改键
- 托盘菜单新增对应项

**涉及模块**：`main.ts`、`settings.html`、`settings.ts`、`preload.ts`（暴露 API）
**工作量**：小

---

### P1-1 勿扰/专注模式

**现状**：无，任何时候都通知。

**方案**：
- **手动开关**：托盘菜单"勿扰模式"，开启后状态照常流转但不发通知
- **时段规则**：设置页配置静默时段（如 22:00-08:00），到点自动切换
- **分级静默**：勿扰模式下可选"只通知 waiting"（等待输入可能阻塞 AI，比 done 更紧急）

**涉及模块**：配置新增 `dnd` 字段、`main.ts` 通知发送前过滤、设置页 UI
**工作量**：中

---

### P1-2 通知内容增强

**现状**：标题"XX 已完成任务"，正文"活动已停止，可以回来接管了"——信息量极低。

**方案**：
- **耗时**：aggregator 已有 `workingDuration`，通知正文加"用时 X 分 Y 秒"
- **改动文件数**：文件探针在 working 周期内收集变更文件集合，done 时随事件上报
- **waiting 原因**：Shell Hook 区分"命令结束"vs"等待输入"；Claude jsonl 能检测提问；Codex 拿不到 waiting（已知限制，标注即可）

**涉及模块**：`aggregator.ts` 的 notify payload 扩展、`fileProbe.ts` 收集变更文件、`main.ts` 通知文案
**工作量**：中（需要 core 包的 MonitorEvent 配合扩展字段）

---

### P1-3 通知精细化控制

**现状**：全局统一通知行为。

**方案**：
- **不同状态不同声音**：done / waiting 用不同音效（`soundPlayer` 已支持跨平台）
- **仅失焦时通知**：检测前台应用，如果 AI 工具正在前台则不弹桌面通知（VS Code 扩展已有此逻辑，桌面端缺）
- **按目标开关**：每个监控目标独立的通知开关（比如只关 Claude 的通知）

**涉及模块**：配置扩展、`main.ts` 通知逻辑
**工作量**：小-中

---

### P2-1 远程通知渠道

**现状**：只有本地系统通知，离开电脑就收不到。

**方案**：做两个通用渠道，覆盖 80% 场景，不逐个适配平台：
- **Webhook**：用户配置 URL，done/waiting 时 POST JSON（飞书/钉钉/企微自定义机器人兼容）
- **ntfy.sh**：开源推送，配置 topic 即可，手机装 app 就能收

**涉及模块**：新增 `desktop/src/notifiers/webhook.ts`、`notifiers/ntfy.ts`、配置扩展、设置页 UI
**工作量**：中

---

### P2-2 自定义监控目标

**现状**：5 个目标写死在 `DEFAULT_TARGETS`（VS Code / Cursor / Claude / ChatGPT / Terminal），用户想加 Gemini CLI、Windsurf 等做不到。

**方案**：
- 设置页"监控目标"区域新增"添加自定义目标"
- 用户填：名称、进程名匹配（可选）、工作区目录（可选）
- 底层复用文件探针 + 进程探针，`buildProbes` 已支持动态目标，不需要新探针
- 配置持久化

**涉及模块**：设置页 UI、`config.ts` 类型扩展
**工作量**：中

---

### P2-3 多会话状态管理

**现状**：aggregator 是**单一全局状态机**，多个探针的事件汇入同一个状态。如果 Claude 和 Codex 同时在跑，只能看到一个"工作中"，分不清谁是谁。

**方案**：
- aggregator 从单状态机改为"按来源分组的状态机集合"
- 托盘菜单展示每个活跃会话：来源、状态、已运行时长
- 通知里带上具体会话标识
- **这是架构级改动**，影响聚合引擎、通知、托盘菜单、历史记录，需要单独出设计文档

**涉及模块**：`aggregator.ts` 重构、托盘菜单、历史窗口
**工作量**：大（建议单独评估，不与其他功能混做）

---

### P3-1 统计仪表盘

**现状**：无。

**方案**：
- 基于活动历史数据，展示日/周/月统计
- 指标：总工作时长、任务数、平均时长、各工具占比
- 新窗口展示，用纯 HTML/CSS 柱状图，不引入图表库

**涉及模块**：历史窗口扩展或新增 `statsWindow`
**工作量**：中（依赖 P0-1 的活动历史数据层）

---

## 四、实施路线图

| 里程碑 | 内容 | 预计 |
|---|---|---|
| **M1：体验闭环** | P0 三个功能（历史记录 + 一键跳转 + 自启/快捷键） | 3-4 天 |
| **M2：通知升级** | P1 三个功能（勿扰模式 + 内容增强 + 精细控制） | 3-4 天 |
| **M3：扩展能力** | P2 远程通知 + 自定义目标；多会话单独评估 | 4-5 天 |
| **M4：发布准备** | 灰度验证 + 图标/GIF + Marketplace 上架 + GitHub Release | 2-3 天 |

---

## 五、关键取舍

1. **多会话管理是架构改动**，不跟其他功能混做，建议 M1-M2 完成后单独出设计再动手
2. **远程通知优先 webhook + ntfy**，不逐个适配 Telegram/Bark/PushPlus，通用方案覆盖绝大多数场景
3. **活动历史和统计仪表盘共享数据层**，先做历史，统计是数据的可视化延伸
4. **通知内容增强需要 core 包配合**，`MonitorEvent` 要加可选字段（变更文件集合等），注意向后兼容

---

## 六、变更记录

- 2026-08-26：初始版本，基于竞品对比分析整理出 10 项待迭代功能，按 P0/P1/P2/P3 分级。
- 2026-08-26：M1 体验闭环完成。P0 三项全部落地：活动历史（activityLog + historyWindow）、一键跳转（focusApp + 通知点击回调）、开机自启 + 全局快捷键（设置页可配）。构建通过，67 单测全绿。
- 2026-08-26：M2 通知升级完成。P1 三项全部落地：勿扰模式（手动开关 + 定时时段 + onlyWaiting）、通知内容增强（耗时 + 改动文件数）、通知精细化控制（done/waiting 不同系统声音 + 仅失焦通知 + 按目标独立通知开关）。构建通过，67 单测全绿。
- 2026-08-26：M3 扩展能力部分完成。P2-1 远程通知（webhook + ntfy）、P2-2 自定义监控目标（设置页添加/删除）落地。P2-3 多会话状态管理为架构级改动，待单独评估。构建通过，67 单测全绿。
